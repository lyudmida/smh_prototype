// Import libraries for making a component
import React from 'react';
import { Text } from 'react-native';

// Make a component
const Header = () => {
  return <Text>Hallo hallo</Text>;
};

// Make the component available to other parts of the app
export default Header;
